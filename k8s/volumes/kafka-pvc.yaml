apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-data-pvc
  namespace: aurora-logs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3  # Or your preferred storage class
  resources:
    requests:
      storage: 50Gi