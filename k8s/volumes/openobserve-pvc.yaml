apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openobserve-data-pvc
  namespace: aurora-logs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2  # Using existing gp2 storage class
  resources:
    requests:
      storage: 100Gi