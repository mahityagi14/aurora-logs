apiVersion: v1
kind: Service
metadata:
  name: kafka-broker-service
  namespace: aurora-logs
  labels:
    app: kafka-broker
spec:
  type: ClusterIP
  ports:
  - name: broker
    port: 9092
    targetPort: 9092
    protocol: TCP
  - name: controller
    port: 9093
    targetPort: 9093
    protocol: TCP
  selector:
    app: kafka-broker